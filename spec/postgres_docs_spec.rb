# frozen_string_literal: true

RSpec.describe PostgresDocs do
  it "has a version number" do
    expect(PostgresDocs::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
